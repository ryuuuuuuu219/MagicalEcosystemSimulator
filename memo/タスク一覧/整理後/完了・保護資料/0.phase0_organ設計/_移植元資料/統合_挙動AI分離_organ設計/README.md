# 統合_挙動AI分離_organ設計

## 位置づけ

`4.挙動AI分離` と `0.phase0_organ設計` に分かれていた移植元資料を統合した履歴フォルダ。

挙動 AI 分離は、旧 `herbivoreBehaviour` / `predatorBehaviour` に集まっていた視覚、記憶、欲求、移動、攻撃、環境反応を organ 形式へ分解する前段だった。現在は `OrganFoundation` + `AnimalBrain` + `AIComponentSet` を中心にした phase0 organ 設計へ吸収済み。

## 統合元の旧パス

- `../0.phase0_organ設計`
- `../4.挙動AI分離`
- `../26.5.8_挙動AI分離_機能一覧_設計後.md`
- `../26.5.8_挙動AI分離_現状機能と設計方針.md`

## 読む順番

1. `26.5.8_挙動AI分離_現状機能と設計方針.md`
2. `26.5.8_挙動AI分離_機能一覧_設計後.md`
3. `organ定義.md`
4. `organ依存関係.md`
5. `インストーラー確率方針.md`
6. `コンポーネント導入確率表.md`
7. `旧behaviour維持設計.md`
8. `メモ上の矛盾.txt`

## 版違い資料

`*_整理後ルート版.md` は、整理後ルート直下に置かれていた同名資料を保全したもの。内容確認時は、まず無印の資料を正本として読み、差分が必要な場合だけ整理後ルート版を参照する。

## 完了メモ

2026-05-09 時点で、実機プレイにより移動、追跡、逃走、攻撃の二重実行問題は確認されなかった。プレイ中に気になった旋回処理は、速度比に応じた旋回慣性として `AnimalAICommon.ApplyMovement()` / `GroundMotor` 側で対処済み。
