# リリース後TODO（2026-03-25）

初回リリース完了後の、`memo/` 記述と現状差分から作成した実行タスク一覧。

## 判定メモ（現状）

- 初回リリース向けの方針整理（開始導線、最小メニュー、対象外切り分け）は完了済み。
- ユーザー向け文書は `document/ユーザー向け/説明書.txt` が存在する。
- 初回リリース専用フォルダは整理済み（削除済み）。
- 基準 README は `memo/README.md` に統一した。
- リリース後拡張候補は `memo/タスク一覧` と `memo/設定` に分散。
- `memo/設定/環境外乱.txt` が新規作成され、`ゲーム設計.txt` はそこへの参照に更新済み（未コミット）。

## 優先度A（今すぐ）

### 1. ユーザー向け説明書の正式化

- 対応:
  - `document/ユーザー向け/説明書.txt` を正式版へ昇格（名称または本文で版を明確化）
  - 初回導線（起動→生成→観察）の手順と、対象外機能の注意を維持
- 完了条件:
  - ユーザーが 1 ファイルで開始から観察まで到達できる

## 優先度B（次スプリント）

### 2. リリース後UI改善の最小セットを1つに絞って着手

`memo/タスク一覧/メニュー整備予定(長期目標).txt` から、次スプリント対象を以下の順で推奨。

- 第一候補: 動物上ゲージの表示切替 UI
  - 理由: 内部フラグが既にあり、UI露出だけで効果が出る
- 第二候補: 世代更新の詳細設定 UI（まずは相選択 + mutation on/off）
  - 理由: 内部パラメータが既に揃っている
- 第三候補: カメラ追従方式切替 UI
  - 理由: 部分実装を公開導線へ昇格できる

### 3. debug可視化（threat/heat）の公開方針を定義

- 対応:
  - 「開発者専用」か「ユーザー公開」かを明文化
  - 公開する場合の最小UI導線を定義
- 完了条件:
  - `memo/タスク一覧/実装機能候補一覧.txt` に公開方針が追記済み

## 優先度C（設計の統合）

### 4. `memo/設定` の分散項目を実装可能単位へ再整理

- 対象:
  - `環境外乱.txt`
  - `遺伝システム.txt`
  - `群知性.txt`
  - `魔法：属性の考察.txt`
- 対応:
  - 「仕様メモ」ではなく「実装チケット粒度」のタスクリストへ変換
- 完了条件:
  - 各項目に `対象システム / 依存 / 完了条件` がある

## 今回の整理ルール

- 初回リリースまでのファイルは履歴扱い（更新を最小化）
- 今後の実作業は本ファイルを起点に更新
- 完了した項目は該当タスクファイルまたはコミット履歴へ反映する

