初回リリースまでの主な残作業

## 1. 設計同期

完了

- `memo/` 内メモを基準として設計同期済み
- 時間スケール定義を `memo/` 基準で整理済み
- 空間スケール定義を `memo/` 基準で整理済み
- AI 更新モデルの説明を `memo/` 基準で整理済み
- 行動決定アルゴリズムの説明を `memo/` 基準で整理済み
- 資源、energy、heat の説明を `memo/` 基準で整理済み
- 最小要件に不要な長期要素を README / ROADMAP / `memo/` の役割で分離済み

## 2. リリース範囲の確定

- 初回リリースに含める機能一覧を確定する
- 初回リリースに含めない機能一覧を確定する
- UI のうち必須画面を決める
- debug 機能のうち公開してよいものを決める
- リリース時点の「完成条件」を文章化する

## 3. UI の絞り込み

- 世代更新 UI の必須項目を決める
- Genome Viewer の公開範囲を決める
- Genome Injector を初回リリースに含めるか決める
- オブジェクト一覧、注目、状態表示 UI の最低構成を決める
- 動物上ゲージ表示の公開設定を決める
- メニュー構成を初回リリース向けに簡略化する

## 4. 行動と環境場の整理

- 草食の移動要素を文書化する
- 肉食の移動要素を文書化する
- `threat map` をリリース範囲でどう扱うか決める
- `heat field` をリリース範囲でどう扱うか決める
- 行動合成と `threat map` の関係を整理する
- 行動合成と `heat field` の関係を整理する
- 未統合の要素を「今は使っていない」と明記する

## 5. 再現条件の整理

- 初期個体数の基準値を決める
- ワールド seed の扱いを決める
- 生成条件を再現するための手順をまとめる
- 実験時に変更する主要パラメータを一覧化する
- シーン起動直後の観察条件を固定する

## 6. 公開手順の作成

- 初回起動から観察開始までの手順を書く
- オブジェクト選択と追跡の手順を書く
- 世代更新の手順を書く
- debug 表示の見方を書く
- リリース時点で未実装の要素を注意書きとしてまとめる

## 7. 最終確認

- README と ROADMAP の記述差分を確認する
- `memo/` 内の関連メモの参照先を整理する
- 初回リリース向け文書だけで説明が通るか確認する
